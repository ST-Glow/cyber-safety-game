# 成人参与者隐私与知情同意

正式启动页采用成人知情同意。参与者点击同意前不开始四关流程、不录屏、
不写实验事件，也不上传文件；拒绝后页面明确提示可以退出。

## 同意页默认告知

- 目的：研究 AI 学习提示对游戏闯关与学习过程的帮助。
- 采集：匿名编号、游戏操作、答题与关卡结果、提示触发/接受情况，以及
  Godot 游戏 Canvas 录像。
- 不采集：真实姓名、手机号、账号、麦克风、摄像头、桌面、其他窗口或
  其他标签页。
- 使用：仅供研究分析和教学改进，由授权研究人员访问。
- 保存：私有 OSS 保存 12 个月，之后由生命周期规则自动删除。
- 删除：参与者可凭匿名编号联系研究者请求提前删除。
- 自愿性：拒绝或退出不会开始数据采集。

浏览器实现只调用 `canvas.captureStream()` 和 `MediaRecorder`，不调用
`getUserMedia()`、`getDisplayMedia()`。录像不可用时生成
`recording-status.json`，事件与总结仍可保存，并在完成页明确说明降级状态。

正式范围仅支持最新版桌面 Chrome、Edge。研究者开测前需补充自己的联系
方式、伦理审批/项目说明（如适用），并在 OSS 上实际配置 12 个月生命周期，
不能只依赖页面文案。
