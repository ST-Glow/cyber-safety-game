# 成人参与者隐私与知情同意

正式启动页采用成人知情同意。参与者必须确认年满 18 岁并点击同意；在此之前
不进入任务、不录屏、不写实验事件，也不上传文件。拒绝后页面明确提示可以退出。

## 同意页告知内容

- 目的：研究 AI 学习提示对 DigComp 游戏任务与学习过程的帮助。
- 流程：约 20–40 分钟，包含综合闯关、拼图、数据修复匹配和图像风险判断。
- 采集：匿名编号、游戏操作、答题与任务结果、提示触发/接受情况，以及 Godot
  游戏 Canvas 录像。
- 不采集：真实姓名、手机号、账号、麦克风、摄像头、桌面、其他窗口或标签页。
- 使用：仅供研究分析和教学改进，由授权研究人员访问。
- 保存：私有 OSS 保存 12 个月，之后由生命周期规则自动删除。
- 删除：参与者可凭匿名编号联系页面展示的研究专用邮箱请求提前删除。
- 自愿性：拒绝、关闭页面或中途退出不会受到处罚；拒绝不会开始数据采集。
- 项目说明：项目方已确认本项目无需单独伦理审批编号；页面不虚构编号。

正式导出必须通过 `-ResearchContact` 或 `RESEARCH_CONTACT` 注入真实、公开的研究
联系及删除请求邮箱。仓库不保存私密凭据，缺少该邮箱时生产导出应失败。

## 录屏和降级

浏览器实现只调用 `canvas.captureStream()` 和 `MediaRecorder`，不调用
`getUserMedia()` 或 `getDisplayMedia()`。默认约 1.5 Mbps、30 FPS，录制分块定期
保存到 IndexedDB，单个录像最多 1 GiB。刷新后待上传数据可恢复；上传成功且
服务端 `manifest.json` 已生成后才清理本地副本。

录像不可用、编码失败或超过限制时，系统生成 `recording-status.json`，事件与
总结仍可保存，并在完成页明确显示降级原因，不会把降级伪装成录像成功。

## 上线前人工核验

正式范围仅支持年满 18 岁的最新版桌面 Chrome、Edge。研究者开测前必须：

1. 填入并测试研究专用邮箱。
2. 在阿里云控制台确认 Bucket 为私有、RAM 为最小权限。
3. 确认 OSS 12 个月生命周期规则已经实际生效，而非只写在页面文案中。
4. 使用无票据、过期票据和拒绝同意三种路径验证不会录屏、记录或上传。
